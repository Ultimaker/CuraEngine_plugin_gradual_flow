{
  "name": "{{ cura_plugin_name }}",
  "author": "{{ author }}",
  "version": "{{ version }}",
  "minimum_cura_version": "5.5",
  "description": "Smooth flow gradually to limit high-flow jumps",
  "api": "{{ api_version }}",
  "supported_sdk_versions": [{% for sdk in sdk_versions %} "{{ sdk }}"{% if not loop.last %}, {% endif %}{% endfor %}],
  "i18n-catalog": "cura"
}